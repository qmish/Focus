feat: Admin Panel Phase 2 — боты, глобальная тема, аудит, политики, аналитика

Часть A: Полноценное управление ботами
- BotEngine wiring: runtime-конфигурация из БД (is_enabled, rate_limit, allowed_rooms, commands_json)
- Hot-reload через POST /admin/bots/reload, DELETE /admin/bots/:id, GET /bots/:id/stats
- Поддержка custom static-reply команд
- BotsPage: inline-edit, JSON-редактор команд, удаление, ошибки, статистика

Часть B: Глобальная тема для чата и конференций
- Модель app_settings (singleton): theme_mode, accent/bg/text цвета, conference_theme_json
- GET /settings/appearance (публичный), PUT /admin/settings/appearance
- Chat ThemeProvider + light/dark CSS, автоприменение палитры из API
- Branding handler мержит conference_theme_json в /branding/jitsi
- SettingsPage: секции «Тема чата» и «Тема конференций»

Часть C: Новые модули
- Аудит-лог: модель audit_logs, recordAudit middleware, GET /admin/audit, AuditPage + CSV экспорт
- Вебхуки: WebhooksPage для просмотра доставок/ошибок
- Политики конференций: модель conference_policies, GET/PUT /admin/conference/policies, ConferencePoliciesPage
- Аналитика: GET /admin/analytics?days=N, AnalyticsPage с bar chart
